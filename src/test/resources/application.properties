# Test configuration for CartEndpointIntegrationTest
# Configure mock catalog service to avoid external HTTP calls during tests

%test.quarkus.http.port=8081
%test.quarkus.rest-client.catalogService.url=http://localhost:9999